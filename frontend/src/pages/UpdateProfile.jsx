/* eslint-disable no-unused-vars */
import React from 'react';

const UpdateProfile = () => {
    return (
        <div>
            Update Profile
        </div>
    );
};

export default UpdateProfile;